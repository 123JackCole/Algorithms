/**
 * @param {string} a
 * @param {string} b
 * @return {string}
 */
 const addBinary = (a, b) => {
    
    lengthA = a.length;
    lengthB = b.length;
  
    const longestLength = Math.max(lengthA, lengthB);

    let counter = 0,
        binarySum = "",
        currSum;
    
    while (counter < longestLength) {
        
        
        
    }
    
};